import LoginCard from "../components/loginCard"

function login() {
  return (
    <div className="bg-[var(--primary-color)] w-screen h-screen flex place-items-center justify-center">
      <LoginCard />
    </div>
  )
}

export default login
